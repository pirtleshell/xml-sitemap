'use strict';
const should = require('chai').should(); // eslint-disable-line no-unused-vars

describe('Option Handling Methods', () => {
  require('./custom-options');
  require('./set-and-get');
  require('./lastmod');
  require('./file-linking');
});
